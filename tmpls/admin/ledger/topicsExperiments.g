{{ template "header" .}}
{{ template "ledgerheader" .}}
<div class="card"><div class="card-header">实验状态与结果</div><div class="card-body"><p>此页面只读。实验由系统操作员通过 <code>cmd/report-experiment</code> 创建和变更状态；结果来自已登记曝光的追加式、幂等观测值，到期后由受控保留任务删除。实验不会自动修改出价或预算。</p><div class="table-responsive"><table class="table table-sm table-hover"><thead><tr><th>ID / 版本</th><th>名称</th><th>所有者</th><th>状态</th><th>主指标：记录数 / 合计</th><th>护栏指标：记录数 / 合计</th><th>保留 / 开始 / 结束</th><th>分组结果</th><th>曝光 / 最近曝光</th><th>停止原因</th></tr></thead><tbody>{{range .Lists}}<tr><td>{{.experiment_id}} / {{.experiment_version}}</td><td>{{.experiment_name}}</td><td>{{.owner_type}} {{.adv_id}}</td><td>{{.status}}</td><td>{{.primary_metric}}: {{.primary_outcomes}} / {{.primary_value}}</td><td>{{.guardrail_metric}}: {{.guardrail_outcomes}} / {{.guardrail_value}}</td><td>{{.retention_hours}} 小时 / {{.starts_at}} / {{.ends_at}}</td><td>{{.variant_results}}</td><td>{{.exposures}} / {{.last_exposure_at}}</td><td>{{.stop_reason}}</td></tr>{{end}}</tbody></table></div><p>分组结果依次显示分配基点、曝光数、主指标记录数/合计和护栏指标记录数/合计。</p></div></div>
{{ template "footer" }}
